# Build output
bin/
obj/
publish/
.vs/
*.user

# Local editor/tool config, not project source
.claude/

# Installer build artifacts (regenerate via installer/build-installer.ps1)
installer/*.msi
installer/*.wixpdb
installer/*.zip
installer/.wix/

# Browser extension build artifact (regenerate by zipping manifest.json/*.js)
browser-extension/*.xpi
browser-extension/*.zip

# Runtime-generated files that should never be committed
SimBriefRaw.json
TicketTemplate.txt
preferences.json
