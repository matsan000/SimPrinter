# Build output
bin/
obj/
publish/
.vs/
*.user

# Local editor/tool config, not project source
.claude/

# Installer build artifacts (regenerate via installer/build-installer.ps1)
installer/*.msi
installer/*.wixpdb
installer/*.zip
installer/.wix/

# Stream Deck plugin npm dependencies (restore via npm install)
streamdeck-plugin/**/node_modules/

# Runtime-generated files that should never be committed
SimBriefRaw.json
TicketTemplate.txt
preferences.json
